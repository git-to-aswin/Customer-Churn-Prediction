# Core
pandas>=2.0
numpy>=1.24
scipy>=1.10
openpyxl>=3.1          # read raw_data/Telco_customer_churn.xlsx

# Modelling
scikit-learn>=1.3      # HistGradientBoostingClassifier is the default model

# Analysis / notebooks
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
nbconvert>=7.0
shap>=0.44             # optional: signed feature attribution

# Optional — faster gradient boosting. On macOS also needs: brew install libomp
# lightgbm>=4.0
